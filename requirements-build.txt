# ARK: Survival Ascended Server Manager - 打包依赖
# 安装: pip install -r requirements-build.txt

# 核心依赖
-r requirements.txt

# 打包工具
pyinstaller>=6.0.0
setuptools>=65.0
wheel>=0.40.0

# 可选：UPX 压缩 (需要单独下载 UPX)
# upx>=4.0.0
